<h2>商品一覧</h2>
<% @items.each do |item| %>
  <div>
    <p><%= attachment_image_tag item, :image, format: 'jpeg', size: "100x100" %></p>
    <p><%= item.name %></p>
    <p><%= item.price %></p>
  </div>
<% end %>