<h2>ジャンル編集</h2>
<%= form_with model: @genre, url: edit_admin_genre_path, local: true do |f| %>
  <div>
    <%= f.label :name, 'ジャンル' %>
    <%= f.text_field :name %>
    <%= f.submit '変更を保存' %>
  </div>
<% end %>